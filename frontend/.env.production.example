# Production on Vercel: leave VITE_API_BASE unset so the app calls /api on the same domain.
# Vercel proxies /api to Render (see vercel.json).
#
# Only set this if you are NOT using the Vercel /api proxy:
# VITE_API_BASE=https://campus360-api-ne29.onrender.com/api
